describe('a test suite to know if the testing framework works', () => {
    test('should know that true is true', () => {
        expect(true).toEqual(false); // Whoops, fix this to make things pass.
    });
});