import { expect } from "chai";

describe('A test suite to determine if the testing environment works', () => {
    it('should know true is true', () => {
        expect(true).to.be.false; // Whoops fix this line to make it pass.
    });
});