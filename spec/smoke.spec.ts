describe('a test suite to know if the testing framework works', () => {
    it('should know that true is true', () => {
        expect(true).toBe(false); // Whoops, fix this to make things pass.
    });
});